name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:latest
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}  # Using the secret here
          MYSQL_USER: ${{ secrets.DB_USERNAME }}  # Using the secret here
          MYSQL_PASSWORD: ${{ secrets.DB_PASSWORD }}  # Using the secret here
          MYSQL_DATABASE: db_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=10s
          --health-retries=10

    steps:
    - uses: actions/checkout@v3
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.3  # Ensure this matches the Ruby version you want to use
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install libmysqlclient-dev
        bundle install
    - name: Start MySQL
      run: |
        sudo service mysql start
    - name: Configure Database
      run: |
        echo "DATABASE_URL=mysql2://root:password@localhost:3306/EventManager_test" >> $GITHUB_ENV
    - name: Setup Database
      run: |
        bundle exec rake db:create
        bundle exec rake db:schema:load
    - name: Run Tests
      env:
        RAILS_ENV: test
      run: |
        bundle exec rspec
